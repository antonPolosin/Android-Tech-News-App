<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anton.techy">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainFragment"
            android:configChanges="orientation|keyboard|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewsController.AiMlNews"
            android:configChanges="orientation|keyboard|screenSize"></activity>
        <activity android:name=".NewsController.AndroidNews"
            android:configChanges="orientation|keyboard|screenSize"></activity>
        <activity android:name=".NewsController.IosNews"
            android:configChanges="orientation|keyboard|screenSize"></activity>
        <activity android:name = ".NewsController.LinuxNews"
            android:configChanges="orientation|keyboard|screenSize"></activity>
        <activity android:name = ".NewsController.WebDesignNews"
            android:configChanges="orientation|keyboard|screenSize"></activity>
        <activity android:name = ".NewsController.GamingNews"
            android:configChanges="orientation|keyboard|screenSize"></activity>
        <activity android:name = ".NewsController.CryptocurrencyNews"
            android:configChanges="orientation|keyboard|screenSize"></activity>
        <activity android:name = ".NewsController.ScienceNews"
            android:configChanges="orientation|keyboard|screenSize"></activity>
        <activity android:name = ".NewsController.MicrosoftNews"
            android:configChanges="orientation|keyboard|screenSize"></activity>


        <activity android:name=".WebViewActivity"></activity>
    </application>

</manifest>